#name: Generate Deck
#
#on:
#  push:
#    paths:
#      - 'slides/misc/**-deck-context.yml'
#  workflow_dispatch:
#    inputs:
#      deck_context:
#        description: 'Path to the deck-context.yml file'
#        required: true
#        default: 'slides/misc/example-deck-context.yml'
#      ai_provider:
#        description: 'AI provider (gemini | mistral | huggingface)'
#        required: false
#        default: 'gemini'
#
#jobs:
#  generate:
#    runs-on: ubuntu-latest
#    # No services: block needed — PgVectorService starts the pgvector container
#    # directly via the Docker API (docker-java). Docker is available on all
#    # ubuntu-latest runners out of the box.
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#        with:
#          token: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Set up JDK 25
#        uses: actions/setup-java@v4
#        with:
#          java-version: '25'
#          distribution: 'temurin'
#
#      - name: Setup Gradle
#        uses: gradle/actions/setup-gradle@v4
#
#      # Resolve deck context path:
#      #   - workflow_dispatch -> use the input value
#      #   - push             -> detect the changed *-deck-context.yml file
#      - name: Resolve deck context path
#        id: context
#        run: |
#          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
#            echo "path=${{ inputs.deck_context }}" >> $GITHUB_OUTPUT
#            echo "provider=${{ inputs.ai_provider }}" >> $GITHUB_OUTPUT
#          else
#            CHANGED=$(git diff --name-only HEAD~1 HEAD -- 'slides/misc/*-deck-context.yml' | head -n 1)
#            echo "path=$CHANGED" >> $GITHUB_OUTPUT
#            echo "provider=gemini" >> $GITHUB_OUTPUT
#          fi
#
#      - name: Generate deck
#        env:
#          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
#          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
#          HUGGINGFACE_TOKEN: ${{ secrets.HUGGINGFACE_TOKEN }}
#        run: |
#          ./gradlew generateDeck \
#            -Pdeck.context=${{ steps.context.outputs.path }} \
#            -Pai.provider=${{ steps.context.outputs.provider }}
#
#      - name: Compile slides
#        run: ./gradlew asciidoctorRevealJs
#
#      - name: Commit generated files
#        run: |
#          git config user.name  "github-actions[bot]"
#          git config user.email "github-actions[bot]@users.noreply.github.com"
#          git add slides/misc/*.adoc build/docs/asciidocRevealJs/ || true
#          git diff --cached --quiet || git commit -m "chore: generate deck from ${{ steps.context.outputs.path }} [skip ci]"
#          git push